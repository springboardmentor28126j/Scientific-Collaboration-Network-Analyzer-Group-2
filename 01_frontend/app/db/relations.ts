// Relations can be added here as the app grows.
// Currently, posts and contacts are standalone tables.
// Example:
// import { relations } from "drizzle-orm";
// import { users, posts } from "./schema";
// export const usersRelations = relations(users, ({ many }) => ({
//   posts: many(posts),
// }));
